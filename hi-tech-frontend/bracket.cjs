const fs = require("fs"); let code = fs.readFileSync("src/pages/Home.jsx", "utf8"); let p=[], b=[], c=[]; for(let i=0; i<code.length; i++){ if(code[i]==="(")p.push(i); if(code[i]===")")p.pop(); if(code[i]==="{")c.push(i); if(code[i]==="}")c.pop(); if(code[i]==="[")b.push(i); if(code[i]==="]")b.pop(); } console.log("(", p.length, "{", c.length, "[", b.length); if(p.length) console.log("Unclosed ( at indexes:", p); if(c.length) console.log("Unclosed { at indexes:", c);
