const fs = require("fs"); const code = fs.readFileSync("src/pages/Home.jsx", "utf8"); let stack = []; let m; const rx = /<(\/)?([a-zA-Z0-9]+)[^>]*?(?<!\/)>/g; while ((m = rx.exec(code)) !== null) { if (m[1] === "/") { let last = stack.pop(); if (last !== m[2]) { console.log(`Mismatch! Expected ${last}, got ${m[2]} at idx ${m.index}`); break; } } else { stack.push(m[2]); } } console.log("Stack:", stack);
